<resources>
    <!-- Base application theme (Universal Android Theme - Guaranteed to compile) -->
    <style name="Theme.AldefSystem" parent="@android:style/Theme.Material.Light.NoActionBar">
        <!-- Status bar and navigation bar styling -->
        <item name="android:statusBarColor">#0F172A</item>
        <item name="android:navigationBarColor">#0F172A</item>
    </style>
</resources>